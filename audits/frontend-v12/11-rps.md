# RPS Frontend QA

Status: Pass

Frontend mapping:
- Uses `/api/bet`.
- Sends `pick: rpsPick`.
- UI lets user choose rock, paper, or scissors.

Backend match:
- RPS engine expects pick and generates house choice server-side.
- Backend tests passed.

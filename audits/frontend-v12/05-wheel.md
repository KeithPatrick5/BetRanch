# Wheel Frontend QA

Status: Pass

Frontend mapping:
- Uses `/api/bet` with default Wheel params.
- Visual stage shows wheel result multiplier from latest Wheel bet.

Backend match:
- Wheel engine uses server-generated segment and payout table.
- Backend tests passed.

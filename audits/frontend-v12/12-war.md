# War Frontend QA

Status: Pass

Frontend mapping:
- Uses `/api/bet` with default War params.
- Visual stage shows card-versus-card layout.

Backend match:
- War engine draws two server-side cards and compares ranks.
- Backend tests passed.

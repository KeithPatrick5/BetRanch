# Limbo Frontend QA

Status: Pass

Frontend mapping:
- Uses `/api/bet`.
- Sends `target: limboTarget`.
- UI has editable target multiplier input.
- Visual stage displays target/result multiplier.

Backend match:
- Limbo engine expects target multiplier.
- Backend tests passed.
